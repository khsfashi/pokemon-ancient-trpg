{
  "name": "pokemon-ancient-trpg",
  "version": "0.0.0-p7",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build && node tools/build-service-worker.mjs && node tools/validate-dist.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "preact": "10.29.7"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "6.0.3",
    "vite": "8.0.16",
    "vitest": "4.1.10",
    "workbox-build": "7.4.1"
  }
}
